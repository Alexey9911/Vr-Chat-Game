{
  "name": "alonhouse-realtime",
  "version": "0.1.0",
  "private": true,
  "description": "WebSocket (TCP) realtime relay for alonHouse (single-lobby presence + chat + app events + voice).",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
